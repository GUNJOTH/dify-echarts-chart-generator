# Privacy policy

This plugin processes chart parameters only in the Dify plugin runtime. It does not collect, transmit, retain, or share user data. When the optional standalone HTML output is opened by a user, that HTML loads the ECharts JavaScript library from jsDelivr; this browser request is outside the plugin runtime and is governed by the relevant CDN provider's policies.
