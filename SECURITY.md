# 安全策略

## 不要提交的内容

不得提交 API Key、访问令牌、密码、Cookie、私钥、数据库凭据、真实生产数据、个人信息、未脱敏日志、模型权重或内部服务地址。

## 报告漏洞

发现未授权访问、凭据泄露、敏感数据外发、文件上传绕过或供应链风险时，不要创建公开 Issue 或 Pull Request。优先使用 GitHub Private vulnerability reporting；未启用时请通过维护者私密渠道报告，并只提供脱敏复现信息。

## 运行时建议

生产部署应使用安全的 Secret 管理，限制网络出口和 CORS，启用必要的认证授权，定期轮换凭据，并审计上传、配置修改、模型调用和发布操作。未配置认证时，不应把管理接口暴露到不受信任的网络。